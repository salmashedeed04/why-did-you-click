Drop your video files in this folder using these EXACT names.
The website already looks for them — no code changes needed.

level1.mp4       → Level 1 feed video (the "secret double life" clip)
level3-top.mp4   → Level 3 top half (gameplay footage)
level3-bottom.mp4→ Level 3 bottom half (narrated story footage)
level5-a.mp4     → Level 5, Clip A ("morning routine")
level5-b.mp4     → Level 5, Clip B ("reacting to news") — this is the one marked as AI-generated
level5-c.mp4     → Level 5, Clip C ("studying with me")

Tips:
- Keep each file small (under ~10MB) so the page loads fast, especially on phones.
- MP4 (H.264) format works everywhere. If your file is .mov, convert it to .mp4 first.
- If a file is missing, that slot just falls back to the original gradient design —
  nothing breaks, so you can add these one at a time.
- When uploading to GitHub: create a folder literally named "videos" in your repo
  (same level as index.html), and upload the files there with these exact names.
